# Account and workspace visual refinement

Reviewed the supplied 74-second recording and centered dark account reference. Replaced the split account composition with a centered dark card, blue headline, light beams and curved light trails. Uses TIPIX charcoal/indigo/blue, not the reference's green. Retains the shared header and footer; this is an adaptation, not a pixel-identical copy.

The new canvas atmosphere has 72 desktop / 38 narrow-screen depth particles, capped 1.5 pixel density, damped cursor response, visibility suspension and resize/listener/frame cleanup. It works without WebGL. Reduced-motion draws a static field and disables CSS movement; an explicit pause control is also available. Workspace entrances are staggered, interactive cards lift, dashboard orbit points move, and flashcards use a reversible 3D flip. Workspace motion can be paused.

Landing source and crystal animation are unchanged. Existing registration validation, required class/stream selection, preview disclosure and local workspace data behavior are preserved. Authentication and durable backend services are still pending.

Validation: production build and all nine existing profile/curriculum tests pass. Visually inspected desktop login, narrow-screen login/signup and desktop flashcards. Narrow signup reported equal document/client widths (375 px inside a 390 px frame with scrollbar). Pause control updates its pressed state. No application errors observed; browser extension metadata errors are unrelated to TIPIX. These checks are not device performance or production load tests.
